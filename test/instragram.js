const assert = require('assert');

describe('Basic Mocha Test', () => {
    it('should be equal instragram', () => {
        const hello = 'Hello World';
        assert.equal(hello, 'Hello World');
    });
});
